# Vitalicia — variables de entorno (plantilla). Copiar a .env y rellenar.
# Las VITE_ se exponen al navegador (son públicas por diseño): solo claves publishable/anon.

VITE_SUPABASE_PROJECT_ID="avlbdqqwldjteafmzwjb"
VITE_SUPABASE_PUBLISHABLE_KEY="<anon / publishable key (pública)>"
VITE_SUPABASE_URL="https://avlbdqqwldjteafmzwjb.supabase.co"

# --- Server / CLI only — NO usar prefijo VITE_ (no debe llegar al navegador) ---
# Token del Management API de Supabase (sbp_...). Uso: CLI, migraciones, deploy de edge functions.
SUPABASE_ACCESS_TOKEN="sbp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# --- Secretos de Edge Functions (se configuran en Supabase, NO aquí) ---
# RMS_API_USER, RMS_API_PASSWORD, RMS_API_KEY, RMS_API_URL y la API key de Resend
# viven como "Function Secrets" en Supabase, nunca en el repo ni en este archivo.
